import React from "react";

function Button_S({ text }) {
  return (
    <button className="w-36 h-[16px] bg-primary-color rounded-[5px] text-white">
      {text}
    </button>
  );
}

export default Button_S;
